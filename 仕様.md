# 写真位置マップ アプリケーション仕様

## 1. 概要

本アプリケーションは、JPEG・WebP写真に保存されたEXIF情報を解析し、撮影日時、
GPS座標、各種EXIFタグを一覧表示する。GPS座標を持つ写真はOpenStreetMap上に
撮影位置を表示する。

同一の`index.html`をWindowsとmacOSのネイティブWebView内で動作させる。
ネイティブウィンドウのタイトルは`PhotoLocationMap`、画面内のアプリ名は
「写真位置マップ」とする。

## 2. 対象OS

| OS | 対象 | WebView |
|---|---|---|
| Windows | Windows 10／11、64bit | Microsoft Edge WebView2 |
| macOS | macOS 12以降 | WebKit |

ソース実行およびビルドにはuvを使用する。uvはPython 3.10以降と依存関係を
一時隔離環境で管理し、プロジェクト内に`.venv`を作成しない。配布用アプリの
利用者にはuvやPythonのインストールを要求しない。

## 3. アプリケーション構成

| ファイル | 役割 |
|---|---|
| `index.html` | UI、ファイル読込、EXIF解析、地図制御 |
| `icon.png` | HTMLファビコンおよびアプリケーションアイコン |
| `main.py` | pywebviewウィンドウの生成とアプリ起動 |
| `build.py` | OS別のPyInstallerパッケージ生成 |
| `build_windows.bat` | uvを使用するWindows用ビルド操作 |
| `build_mac.command` | uvを使用するmacOS用ビルド操作 |
| `requirements.txt` | uvが一時環境へ導入するPython依存関係 |

`main.py`は`index.html`をpywebviewのローカルHTTPサーバーから配信する。
直接`file://`で開かないことで、ローカルファイル固有のWebセキュリティ制限を
避ける。

`main.py`が生成するネイティブウィンドウのタイトルは`PhotoLocationMap`とする。

## 4. 画面仕様

### 4.1 ヘッダー

- アプリ名と`icon.png`を表示する。
- 「フォルダを選択」でフォルダ内の対象画像を読み込む。
- 「ファイルを選択」で複数の対象画像を読み込む。
- 読込中は「検索を中止」を有効にする。
- サブフォルダ検索の有効・無効を指定できる。

### 4.2 写真一覧

- 写真一覧領域全体をドラッグ＆ドロップ受付領域とする。
- ドラッグ中はドロップ可能であることを強調表示する。
- サムネイル、ファイル名、撮影日時、GPS状態を表示する。
- ファイル名または撮影日時で絞り込める。
- 選択行を強調表示する。
- 未選択行のクリックでは写真を選択して地図マーカーを移動する。
- 選択済みの行を再度クリックした場合は、マーカーを移動せずEXIF情報を表示する。

### 4.3 地図

- GPS情報を持つ選択写真の座標をOpenStreetMapに表示する。
- 選択地点にマーカーと写真情報を表示する。
- GPS情報がない場合は案内を表示する。
- Googleマップは既定の外部ブラウザで開く。

### 4.4 EXIF情報

- 選択写真のサムネイル、撮影日時、GPS状態を表示する。
- 基本情報、主要情報、EXIFタグを読み取り専用表で表示する。
- 写真ファイル自体は更新しない。

## 5. ファイル読込仕様

- 対象拡張子：`.jpg`、`.jpeg`、`.webp`
- 拡張子の大文字・小文字は区別しない。
- 複数ファイル、フォルダ、サブフォルダに対応する。
- D&Dのディレクトリエントリは、空の結果が返るまで分割取得を繰り返す。
  これによりブラウザAPIの1回約100件の取得上限を回避する。
- EXIF解析中は20件ごとにUIへ制御を戻し、キャンセル操作を受け付ける。
- 読込エラーは写真単位で処理し、他の写真の読み込みを継続する。
- 再読込時とキャンセル時はサムネイル用Object URLを解放する。

## 6. ネットワーク仕様

以下の用途でインターネット接続を使用する。

- Leaflet JavaScript／CSSの取得
- exifrライブラリの取得
- OpenStreetMap地図タイルの取得
- Googleマップを外部ブラウザで開く
- Google Fontsの取得

写真データとEXIFデータはローカルのWebView内で処理し、地図サービスへ
アップロードしない。地図サービスには表示対象の座標に対応する地図タイルの
リクエストが送信される。

## 7. パッケージ仕様

### Windows

- 形式：単一EXE
- 出力：`dist/PhotoLocationMap.exe`
- ネイティブウィンドウタイトル：`PhotoLocationMap`
- コンソールウィンドウ：非表示
- アイコン：`icon.png`からPyInstallerとPillowがWindows形式へ変換
- `build_windows.bat`はuvの一時隔離環境でビルドする
- ビルド成功後に`uv cache prune`を実行する

### macOS

- 形式：`.app`バンドル
- 出力：`dist/PhotoLocationMap.app`
- バンドルID：`net.nfeed.photo-location-map`
- アイコン：`icon.png`からPyInstallerとPillowがmacOS形式へ変換
- 実ファイル名：署名時のUnicodeパス問題を避けるため英数字名を使用
- ネイティブウィンドウタイトル：`PhotoLocationMap`
- 画面内表示名：「写真位置マップ」
- `build_mac.command`はuvの一時隔離環境でビルドする
- ビルド前に素材のmacOS拡張属性を除去
- ローカル実行用のアドホック署名を付与
- ビルド成功後に`uv cache prune`を実行する

PyInstallerはクロスコンパイラではないため、Windows版はWindows、Mac版は
macOSでビルドする。

両ビルドスクリプトは次の方式で`requirements.txt`を読み込む。

```text
uv run --isolated --no-project --python ">=3.10" --with-requirements requirements.txt build.py
```

`uv cache prune`は不要なキャッシュと一時環境を削除するが、再利用可能な
パッケージキャッシュは残る場合がある。全キャッシュの削除は自動実行しない。

## 8. エラー処理

- `index.html`が存在しない場合は起動を中止し、欠落ファイルを示す。
- 対象写真がない場合は画面上で通知する。
- EXIF解析に失敗した写真はGPS状態を「エラー」とする。
- ビルドに必要なファイルがない場合は、対象ファイル名を表示して中止する。
- uvが見つからない場合、ビルドスクリプトは導入方法を表示して中止する。
- ビルドまたは`uv cache prune`が失敗した場合、Windowsスクリプトは失敗終了する。

## 9. 制限事項

- HEIC、PNG、RAW、動画には対応しない。
- 地図とCDNライブラリはオフラインでは利用できない。
- Apple Developer IDによる正式な配布用署名、公証、DMG作成はビルド処理に含まない。
- Windows WebView2 Runtimeがない環境では別途導入が必要となる。
- uv管理のPython本体はuvキャッシュとは別に保持される場合がある。
